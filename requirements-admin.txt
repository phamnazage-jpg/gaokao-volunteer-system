# T6.1 管理后台运行依赖
# 与 requirements-dev.txt 分离，避免把 FastAPI 运行栈混入纯标准库主链路。
# 安装示例：pip install -r requirements-admin.txt -r requirements-dev.txt

fastapi==0.133.1
uvicorn==0.41.0
PyJWT==2.10.1
pydantic==2.13.4
cryptography>=43,<46
